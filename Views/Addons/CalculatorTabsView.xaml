<UserControl x:Class="TWChatOverlay.Views.Addons.CalculatorTabsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="CalculatorTabItemStyle"
               TargetType="TabItem"
               BasedOn="{StaticResource SettingsTabItemStyle}">
            <Setter Property="MinWidth" Value="190"/>
            <Setter Property="MinHeight" Value="46"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#161B22" Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TabControl x:Name="CalculatorTabControl"
                    Grid.Row="0"
                    Background="Transparent"
                    BorderThickness="0"
                    SelectionChanged="CalculatorTabControl_SelectionChanged">
            <TabItem Header="계수 계산기" Tag="Coefficient" Style="{StaticResource CalculatorTabItemStyle}" Margin="0,0,0,0"/>
            <TabItem Header="대미지 계산기" Tag="Damage" Style="{StaticResource CalculatorTabItemStyle}" Margin="0,0,0,0"/>
        </TabControl>

        <Border Grid.Row="2"
                Background="#101720"
                BorderBrush="#2E3A4A"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8">
            <ContentControl x:Name="CalculatorContentHost"/>
        </Border>
    </Grid>
</UserControl>
