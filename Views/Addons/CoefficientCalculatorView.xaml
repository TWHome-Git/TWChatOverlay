<UserControl x:Class="TWChatOverlay.Views.Addons.CoefficientCalculatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Background="#161B22">
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="31*"/>
        </Grid.RowDefinitions>
        <ScrollViewer x:Name="CharacterSelectView" VerticalScrollBarVisibility="Auto" Grid.RowSpan="2">
            <ItemsControl x:Name="CharacterListControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Margin="20" HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Click="Character_Click" Tag="{Binding Name}" Margin="10" Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <StackPanel>
                                        <Border x:Name="CharBorder" CornerRadius="12" BorderBrush="#30363D" BorderThickness="1" Background="#161B22">
                                            <Grid ClipToBounds="True">
                                                <Image Source="{Binding ImagePath}"
                                                       Width="150"
                                                       Height="150"
                                                       Stretch="UniformToFill"
                                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                            </Grid>
                                        </Border>
                                        <TextBlock Text="{Binding Name}"
                                                   Foreground="White"
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,8,0,0"/>
                                    </StackPanel>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="CharBorder" Property="BorderBrush" Value="#58A6FF"/>
                                            <Setter TargetName="CharBorder" Property="Background" Value="#1C2128"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid x:Name="CalculatorDetailView" Visibility="Collapsed" Background="#161B22" Grid.RowSpan="2">

            <TextBlock x:Name="SelectedCharName" Foreground="#58A6FF" FontSize="20" HorizontalAlignment="Center" Margin="10" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</UserControl>
