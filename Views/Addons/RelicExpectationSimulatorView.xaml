<UserControl x:Class="TWChatOverlay.Views.Addons.RelicExpectationSimulatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Background="{DynamicResource OverlaySurfaceBackgroundBrush}" Margin="0,8,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="430"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource OverlaySurfaceBackgroundBrush}" Padding="16,12,12,12">
            <StackPanel>
                <TextBlock Text="신조 렐릭 기대값" Foreground="{DynamicResource OverlayAccentTextBrush}" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16"/>

                <TextBlock Text="장비 종류" Foreground="{DynamicResource OverlaySubtleTextBrush}" FontSize="14" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,14">
                    <RadioButton x:Name="PendantRadio" Content="펜던트" IsChecked="True" Foreground="{DynamicResource OverlayInfoTextBrush}" FontSize="13" Margin="0,0,18,0"/>
                    <RadioButton x:Name="BraceletRadio" Content="브레이슬릿" Foreground="{DynamicResource OverlayInfoTextBrush}" FontSize="13"/>
                </StackPanel>

                <Border Height="1" Background="{DynamicResource OverlayStrongBorderBrush}" CornerRadius="2" Margin="0,0,0,14"/>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="현재 레벨 (0~19)" Grid.Row="0" Grid.Column="0" Foreground="{DynamicResource OverlaySubtleTextBrush}" FontSize="13" Margin="0,0,0,4"/>
                    <TextBlock Text="목표 레벨 (1~20)" Grid.Row="0" Grid.Column="2" Foreground="{DynamicResource OverlaySubtleTextBrush}" FontSize="13" Margin="0,0,0,4"/>
                    <TextBox x:Name="CurrentLevelTextBox" Grid.Row="1" Grid.Column="0" Text="0" Height="34" Style="{StaticResource SimTextBoxStyle}"/>
                    <TextBox x:Name="TargetLevelTextBox" Grid.Row="1" Grid.Column="2" Text="10" Height="34" Style="{StaticResource SimTextBoxStyle}"/>
                </Grid>

                <TextBlock Text="강화 가능 단수 (1~20)" Foreground="{DynamicResource OverlaySubtleTextBrush}" FontSize="13" Margin="0,0,0,4"/>
                <TextBox x:Name="DifficultyTextBox" Text="20" Height="34" Margin="0,0,0,14" Style="{StaticResource SimTextBoxStyle}"/>

                <Button Content="기대값 계산"
                        Click="CalculateButton_Click"
                        Height="38"
                        HorizontalAlignment="Stretch"
                        FontSize="14"
                        Style="{StaticResource SimButtonStyle}"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="10,8,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" BorderBrush="{DynamicResource OverlayStrongBorderBrush}" BorderThickness="1" Background="{DynamicResource OverlaySurfaceBackgroundBrush}" Padding="8" CornerRadius="8" Margin="0,0,0,10">
                <DataGrid x:Name="ResultDataGrid"
                          AutoGenerateColumns="False"
                          Background="{DynamicResource OverlayShellBackgroundBrush}"
                          BorderBrush="{DynamicResource OverlayStrongBorderBrush}"
                          BorderThickness="1"
                          Foreground="{DynamicResource TextBrush}"
                          FontSize="13"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          CanUserSortColumns="False"
                          IsReadOnly="True"
                          RowHeight="30"
                          RowHeaderWidth="0"
                          SnapsToDevicePixels="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          GridLinesVisibility="Horizontal"
                          RowBackground="{DynamicResource OverlayShellBackgroundBrush}"
                          AlternatingRowBackground="{DynamicResource OverlayHighlightBackgroundBrush}"
                          HorizontalGridLinesBrush="{DynamicResource OverlayStrongBorderBrush}"
                          VerticalGridLinesBrush="{DynamicResource OverlayStrongBorderBrush}"
                          ColumnHeaderHeight="28">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{DynamicResource OverlaySurfaceAltBackgroundBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource OverlayStrongBorderBrush}"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="8,2"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource SeparatorBrush}"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                        <Style x:Key="CellTextCenterStyle" TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="단계" Binding="{Binding StageLabel}" Width="0.9*" ElementStyle="{StaticResource CellTextCenterStyle}"/>
                        <DataGridTextColumn Header="확률" Binding="{Binding ChanceText}" Width="0.8*" ElementStyle="{StaticResource CellTextCenterStyle}"/>
                        <DataGridTextColumn Header="시도" Binding="{Binding ExpectedAttemptsText}" Width="0.8*" ElementStyle="{StaticResource CellTextCenterStyle}"/>
                        <DataGridTextColumn Header="신조의 가루" Binding="{Binding PowderText}" Width="1.0*" ElementStyle="{StaticResource CellTextCenterStyle}"/>
                        <DataGridTextColumn Header="신조의 정수" Binding="{Binding EssenceText}" Width="1.0*" ElementStyle="{StaticResource CellTextCenterStyle}"/>
                        <DataGridTextColumn Header="달의 파편" Binding="{Binding MoonPieceText}" Width="0.9*" ElementStyle="{StaticResource CellTextCenterStyle}"/>
                        <DataGridTextColumn Header="월광석" Binding="{Binding MoonStoneText}" Width="0.8*" ElementStyle="{StaticResource CellTextCenterStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Row="1" Background="{DynamicResource OverlaySurfaceBackgroundBrush}" BorderBrush="{DynamicResource OverlayStrongBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                <TextBlock x:Name="SummaryTextBlock"
                           Foreground="{DynamicResource OverlayInfoTextBrush}"
                           FontSize="14"
                           LineHeight="22"
                           TextWrapping="Wrap"/>
            </Border>
        </Grid>
    </Grid>
</UserControl>
