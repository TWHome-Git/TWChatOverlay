<Window x:Class="TWChatOverlay.Views.ExpTrackerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:TWChatOverlay.Views"
        Title="Experience Tracker"
        Height="42"
        MinWidth="220"
        MinHeight="42"
        SizeToContent="Width"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border x:Name="DragBar"
                Grid.Row="0"
                Background="{DynamicResource OverlayDragBarBackgroundBrush}"
                Height="18"
                Visibility="Collapsed"
                MouseLeftButtonDown="DragBar_MouseLeftButtonDown"
                Cursor="Hand">
            <TextBlock Text="⋮⋮ 설정에서 드래그하여 이동 ⋮⋮"
                       Foreground="{DynamicResource OverlayDragBarTextBrush}"
                       FontSize="10"
                       TextAlignment="Center"
                       VerticalAlignment="Center"
                       FontStyle="Italic"/>
        </Border>
        <views:ExpTrackerView Grid.Row="1"/>
    </Grid>
</Window>



