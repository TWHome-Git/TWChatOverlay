<Window x:Class="TWChatOverlay.Views.ExperienceWeeklyRefreshPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="520"
        Height="228"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        ShowActivated="False">
    <Border Background="#F0202229"
            BorderBrush="#FF5AA9E6"
            BorderThickness="1.4"
            CornerRadius="10"
            Padding="14,12"
            MouseLeftButtonDown="RootBorder_MouseLeftButtonDown">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Width="26"
                    Height="26"
                    Click="CloseButton_Click"
                    Background="#33FFFFFF"
                    BorderBrush="#55FFFFFF"
                    BorderThickness="1"
                    Foreground="White"
                    Cursor="Hand"
                    Content="X"/>

            <TextBlock Grid.Row="0"
                       Margin="0,0,0,8"
                       Text="주간 누적 경험치 갱신 안내"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="SemiBold"
                       TextAlignment="Center"
                       HorizontalAlignment="Center"/>

            <TextBlock Grid.Row="1"
                       Margin="4,0,4,8"
                       Text="이번 주 누적 경험치를 억 단위로 입력 후 갱신해주세요."
                       Foreground="#D7E7FF"
                       FontSize="14"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>

            <Grid Grid.Row="2" Margin="40,0,40,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="ExpEokTextBox"
                         Grid.Column="0"
                         Height="30"
                         VerticalContentAlignment="Center"
                         HorizontalContentAlignment="Right"
                         PreviewTextInput="ExpEokTextBox_PreviewTextInput"
                         DataObject.Pasting="ExpEokTextBox_Pasting"/>
                <TextBlock Grid.Column="1"
                           Margin="8,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="White"
                           Text="억"/>
            </Grid>

            <TextBlock x:Name="StatusTextBlock"
                       Grid.Row="3"
                       Margin="0,0,0,10"
                       Foreground="#9FD0FF"
                       FontSize="12"
                       Text="이번 주 누적 경험치 갱신이 필요합니다."
                       TextAlignment="Center"/>

            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <Button x:Name="RefreshButton"
                        Content="현재 누적 경험치 갱신"
                        Width="200"
                        Height="34"
                        Padding="12,4"
                        Click="RefreshButton_Click"
                        Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
