<Window x:Class="TWChatOverlay.Views.RecaptureSupplyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="보급품 탈환"
        Width="294"
        Height="204"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="CenterScreen">
    <Border x:Name="RootBorder"
            Background="#F0181B20"
            BorderBrush="#FF58A6FF"
            BorderThickness="1.4"
            CornerRadius="12"
            Padding="4">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid x:Name="TitleBar" Grid.Row="0" Margin="0,0,0,4" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <TextBlock Text="보급품 탈환"
                           Foreground="White"
                           FontSize="10"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"/>

                <Button x:Name="CloseButton"
                        Width="16"
                        Height="16"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Click="CloseButton_Click"
                        Content="X"
                        Background="#33FFFFFF"
                        BorderBrush="#55FFFFFF"
                        BorderThickness="1"
                        Foreground="White"
                        Cursor="Hand"/>
            </Grid>

            <Border Grid.Row="1"
                    Background="Transparent"
                    BorderBrush="#26FFFFFF"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="4">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                              VerticalScrollBarVisibility="Auto"
                              Background="Transparent">
                    <Image x:Name="SupplyImage"
                           Stretch="Uniform"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           SnapsToDevicePixels="True"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</Window>
