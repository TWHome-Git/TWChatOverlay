<Window x:Class="TWChatOverlay.Views.ItemCalendarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="아이템 획득 내역"
        Width="1430"
        Height="880"
        MinWidth="1320"
        MinHeight="820"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Topmost="True"
        Loaded="Window_Loaded">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="CalendarTitleButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="MinWidth" Value="34"/>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource OverlayWindowBackgroundBrush}"
            BorderBrush="{DynamicResource OverlayWindowBorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="{DynamicResource OverlaySurfaceAltBackgroundBrush}"
                    CornerRadius="10"
                    Padding="10"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel HorizontalAlignment="Left"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Button Content="◀"
                                Width="34"
                                Margin="0,0,6,0"
                                Style="{StaticResource CalendarTitleButtonStyle}"
                                Click="PreviousMonth_Click"/>
                        <Button Content="이번달"
                                Width="76"
                                Margin="0,0,6,0"
                                Style="{StaticResource CalendarTitleButtonStyle}"
                                Click="ThisMonth_Click"/>
                        <Button Content="▶"
                                Width="34"
                                Style="{StaticResource CalendarTitleButtonStyle}"
                                Click="NextMonth_Click"/>
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Right"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <TextBlock Text="{Binding StatusText}"
                                   Foreground="{DynamicResource OverlaySubtleTextBrush}"
                                   FontSize="12"
                                   Margin="0,0,10,0"
                                   Width="180"
                                   TextAlignment="Right"
                                   TextTrimming="CharacterEllipsis"
                                   VerticalAlignment="Center"/>
                        <Button Content="갱신"
                                Style="{StaticResource WindowCloseButtonStyle}"
                                Click="Refresh_Click"/>
                        <Button Content="X"
                                Style="{StaticResource WindowCloseButtonStyle}"
                                Click="Close_Click"/>
                    </StackPanel>

                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <TextBlock Text="아이템 획득 내역"
                                   Foreground="White"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding MonthText}"
                                   Foreground="{DynamicResource OverlayInfoTextBrush}"
                                   FontSize="12"
                                   HorizontalAlignment="Center"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3.5*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{DynamicResource OverlayShellBackgroundBrush}"
                        CornerRadius="10"
                        Padding="10"
                        Margin="0,0,10,0">
                    <Grid>
                        <ItemsControl ItemsSource="{Binding Days}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="7"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="4"
                                            Padding="10"
                                            CornerRadius="10"
                                            BorderThickness="1"
                                            BorderBrush="{DynamicResource OverlayCardBorderBrush}"
                                            Background="{DynamicResource OverlayCardBackgroundBrush}"
                                            Opacity="{Binding CellOpacity}">
                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                            </Grid.RowDefinitions>

                                            <Border Background="Transparent"
                                                    BorderBrush="{DynamicResource OverlayCardBorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="8"
                                                    Padding="6,4">
                                                <TextBlock Text="{Binding DateLabel}"
                                                           Foreground="WhiteSmoke"
                                                           FontSize="10"
                                                           FontWeight="Bold"
                                                           VerticalAlignment="Center"/>
                                            </Border>

                                            <ScrollViewer Grid.Row="1"
                                                          Margin="0,10,0,0"
                                                          VerticalScrollBarVisibility="Hidden"
                                                          HorizontalScrollBarVisibility="Disabled"
                                                          CanContentScroll="False"
                                                          VerticalAlignment="Stretch"
                                                          Padding="0,0,0,20">
                                                <ItemsControl ItemsSource="{Binding Entries}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{DynamicResource OverlayCardBackgroundBrush}"
                                                                    BorderBrush="{Binding BorderBrush}"
                                                                    BorderThickness="1"
                                                                    CornerRadius="6"
                                                                    Padding="6,4"
                                                                    Margin="0,0,0,4">
                                                                <TextBlock Text="{Binding DisplayText}"
                                                                           Foreground="White"
                                                                           FontSize="11"
                                                                           FontWeight="SemiBold"
                                                                           TextWrapping="NoWrap"
                                                                           TextTrimming="CharacterEllipsis"/>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                            </ScrollViewer>
                                            <TextBlock Grid.Row="1"
                                                       Margin="0,0,0,0"
                                                       VerticalAlignment="Bottom"
                                                       Foreground="{DynamicResource OverlayExpAccentBrush}"
                                                       FontSize="10"
                                                       FontWeight="SemiBold"
                                                       Text="{Binding ExperienceEssenceText}"
                                                       Visibility="{Binding HasExperienceEssence, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Border Background="{DynamicResource OverlayLoadingMaskBrush}"
                                CornerRadius="8"
                                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Width="280">
                                <TextBlock Text="{Binding StatusText}"
                                           Foreground="White"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           TextAlignment="Center"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,10"/>
                                <ProgressBar Minimum="0"
                                             Maximum="100"
                                             Value="{Binding LoadProgressValue}"
                                             Height="10"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Column="1"
                        Background="{DynamicResource OverlayShellBackgroundBrush}"
                        CornerRadius="10"
                        Padding="10">
                    <ScrollViewer VerticalScrollBarVisibility="Hidden"
                                  HorizontalScrollBarVisibility="Disabled"
                                  CanContentScroll="False">
                        <StackPanel>
                            <Border Background="{DynamicResource OverlaySurfaceBackgroundBrush}"
                                    BorderBrush="{DynamicResource OverlayCardBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="9"
                                    Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="어밴던 누적 합계"
                                               Foreground="White"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,8"/>

                                    <Border Background="{DynamicResource OverlayCardBackgroundBrush}"
                                            BorderBrush="{DynamicResource OverlayCardBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="8,6"
                                            Margin="0,0,0,8">
                                        <TextBlock Text="{Binding MonthlyAbandonSeedText}"
                                                   Foreground="{DynamicResource OverlayExpAccentBrush}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Border>

                                    <ItemsControl ItemsSource="{Binding MonthlyAbandonSummary}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{DynamicResource OverlayCardBackgroundBrush}"
                                                        BorderBrush="{DynamicResource OverlayStrongBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="7"
                                                        Padding="7,5"
                                                        Margin="0,0,0,5">
                                                    <DockPanel LastChildFill="True">
                                                        <StackPanel Orientation="Horizontal"
                                                                    DockPanel.Dock="Left"
                                                                    VerticalAlignment="Center">
                                                            <Image Source="{Binding IconUri}"
                                                                   Width="14"
                                                                   Height="14"
                                                                   Margin="0,0,6,0"
                                                                   VerticalAlignment="Center"/>
                                                            <TextBlock Text="{Binding DisplayName}"
                                                                       Foreground="{Binding NameForegroundHex}"
                                                                       FontSize="11"
                                                                       FontWeight="SemiBold"
                                                                       TextTrimming="CharacterEllipsis"/>
                                                        </StackPanel>
                                                        <TextBlock Text="{Binding CountText}"
                                                                   Foreground="{Binding CountForegroundHex}"
                                                                   FontSize="11"
                                                                   FontWeight="Bold"
                                                                   HorizontalAlignment="Right"
                                                                   DockPanel.Dock="Right"/>
                                                    </DockPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <Border Background="{DynamicResource OverlaySurfaceBackgroundBrush}"
                                    BorderBrush="{DynamicResource OverlayCardBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="9">
                                <StackPanel>
                                    <TextBlock Text="아이템 득템 누적 합계"
                                               Foreground="White"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,8"/>

                                    <ItemsControl ItemsSource="{Binding MonthlySummary}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{DynamicResource OverlayCardBackgroundBrush}"
                                                        BorderBrush="{Binding BorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="7"
                                                        Padding="7,5"
                                                        Margin="0,0,0,5">
                                                    <DockPanel LastChildFill="False">
                                                        <TextBlock Text="{Binding DisplayName}"
                                                                   Foreground="White"
                                                                   FontSize="11"
                                                                   FontWeight="SemiBold"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   DockPanel.Dock="Left"/>
                                                        <TextBlock Text="{Binding Count, StringFormat=x{0:N0}}"
                                                                   Foreground="{DynamicResource OverlayInfoTextBrush}"
                                                                   FontSize="11"
                                                                   FontWeight="Bold"
                                                                   HorizontalAlignment="Right"
                                                                   DockPanel.Dock="Right"/>
                                                    </DockPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <Border Background="{DynamicResource OverlaySurfaceBackgroundBrush}"
                                    BorderBrush="{DynamicResource OverlayCardBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="9"
                                    Margin="0,10,0,0">
                                <StackPanel>
                                    <TextBlock Text="경험의 정수 누적 합계"
                                               Foreground="White"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Margin="0,0,0,8"/>

                                    <Border Background="{DynamicResource OverlayCardBackgroundBrush}"
                                            BorderBrush="{DynamicResource OverlayCardBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Padding="8,6">
                                        <DockPanel LastChildFill="True">
                                            <StackPanel Orientation="Horizontal"
                                                        DockPanel.Dock="Left"
                                                        VerticalAlignment="Center">
                                                <Image Source="pack://application:,,,/Data/images/Item/경험의 정수.png"
                                                       Width="14"
                                                       Height="14"
                                                       Margin="0,0,6,0"
                                                       VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding MonthlyExperienceEssenceSeedText}"
                                                           Foreground="{DynamicResource OverlayExpAccentBrush}"
                                                           FontSize="11"
                                                           FontWeight="SemiBold"
                                                           TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </DockPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
