<Window x:Class="TWChatOverlay.Views.InitialSetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="초기 설정 마법사"
        Width="760"
        Height="520"
        MinWidth="700"
        MinHeight="460"
        WindowStartupLocation="CenterScreen"
        Background="#0D1117"
        WindowStyle="None"
        ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8B949E"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#30363D"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#30363D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Margin" Value="0,2,0,6"/>
            <Setter Property="MinHeight" Value="24"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#161B22"
                BorderBrush="#30363D"
                BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                Cursor="SizeAll">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,6,0">
                <Button Content="-" Click="MinimizeButton_Click" Style="{StaticResource TitleBarButtonStyle}"/>
                <Button Content="X" Click="CloseButton_Click" Style="{StaticResource WindowCloseButtonStyle}" Margin="0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock x:Name="TitleText" Text="초기 설정" Foreground="#58A6FF" FontSize="26" FontWeight="Bold"/>
            <TextBlock x:Name="ProgressText" Text="1 / 5" Foreground="#8B949E" FontSize="12" Margin="0,6,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="8" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock x:Name="StepTitleText" Text="1. 채팅 로그 위치 설정" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock x:Name="StepDescText" Text="" Foreground="#C9D1D9" FontSize="13" Margin="0,0,0,0" TextWrapping="Wrap" Visibility="Collapsed"/>
                    <TextBlock x:Name="StepDetailText" Text="" Foreground="#8B949E" FontSize="12" TextWrapping="Wrap" Visibility="Collapsed"/>
                </StackPanel>

                <ContentControl x:Name="StepContentHost" Grid.Row="1" Margin="0,6,0,0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="SkipButton" Grid.Column="0" Width="110" Height="34" HorizontalAlignment="Left" Content="건너뛰기" Click="SkipButton_Click"/>
            <Button x:Name="PrevButton" Grid.Column="1" Width="110" Height="34" Margin="8,0,0,0" Content="이전" Click="PrevButton_Click"/>
            <Button x:Name="NextButton" Grid.Column="2" Width="110" Height="34" Margin="8,0,0,0" Content="다음" Click="NextButton_Click"/>
            <Button x:Name="FinishButton" Grid.Column="3" Width="110" Height="34" Margin="8,0,0,0" Content="완료" Click="FinishButton_Click" Visibility="Collapsed"/>
        </Grid>
    </Grid>
    </Grid>
</Window>



